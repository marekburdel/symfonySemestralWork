{% extends 'base.html.twig' %}

{% form_theme form 'form/edit.html.twig' %}

{% block body %}
    <div class="main">
        <h1> Reservation for {{ reservation.getUserInitiator().getUsername()}}  </h1>
        <h2> Room {{ room.getName() }}</h2>
        {{ form_start(form) }}
        {{ form_widget(form) }}
        <button type="submit">Submit</button>
        {{ form_end(form) }}
    </div>
{% endblock %}